apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ting-cert
  namespace: ting
spec:
  issuerRef:
    name: letsencrypt-gateway
    kind: ClusterIssuer
  dnsNames:
  - ting.frontstate.org
