apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ting
resources:
- ../../base
- referencegrant.yaml
# Base already targets cmdbee hostnames + prod cert -- no patches needed.
# The ReferenceGrant lets the kube-system Gateway serve ting-tls via SNI.
